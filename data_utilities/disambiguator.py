"""
def function(book):
    ibid_tracker = ()
    for segment in book:
        links = get_links(segment)
        sham_text_finder(segment)
        re.search for the links in segment to find the order (orig links and sham links)
        devide links into len of tuple section groups

        resolved = ibid_tracker.resolve(link_book, (x,y...))


def sham_text_finder(segment):
    look for (sham .+)

"""